# =============================================================================
# Warba Doors — Production-ready Python dependencies (PostgreSQL-first)
# Target: Python 3.14+, Django 5.x
# Keep this file focused on runtime and ops dependencies. Dev tools are listed below.
# =============================================================================

# Core web framework
Django>=5.2.8,<6.0
djangorestframework>=3.15.2,<4.0

# Database
psycopg[binary]>=3.2.2,<4.0

# Security & configuration
python-decouple>=3.8,<4.0
django-cors-headers>=4.6.0,<5.0

# Static files / hosting
whitenoise>=6.5.0,<7.0
gunicorn>=21.2.0,<22.0

# Background tasks
celery>=5.3.0,<6.0
redis>=5.0.0,<6.0

# Media & imaging
Pillow>=12.0.0,<14.0

# Optional PDF/reporting (keep as opt-in for heavy builds)
reportlab>=4.4.0,<5.0

# Utilities
requests>=2.32.3,<3.0
python-dateutil>=2.9.0,<3.0

# Frontend integrations (Python helpers only). Tailwind and Alpine are managed via npm.
django-htmx>=1.6.0,<2.0
django-tailwind>=3.0.0,<4.0

# Django REST helpers (token/JWT can be added later)
djangorestframework-simplejwt>=5.2.0,<6.0

# =============================================================================
# Developer / CI tools (for local/dev environments — keep these lightweight)
# Install with `pip install -r requirements-dev.txt` in CI or local developer setup.
# =============================================================================
# black>=24.7.0
# isort>=5.12.0
# flake8>=6.0.0
# pre-commit>=3.4.0

# Notes:
# - We intentionally avoid mysqlclient/PyMySQL and sqlite drivers; PostgreSQL is the
#   single supported production database. Use `psycopg[binary]` for portable installs
#   or `psycopg[c]` with system libpq on minimal Linux images.
# - Tailwind and Alpine.js are frontend build tools (node/npm). See README for setup.
